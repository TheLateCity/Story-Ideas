# Story-Ideas
Will be used by GPT

## the-late-city

Source of the Ghost theme **The Late City** (`the-late-city/`). Zip the
contents of that directory (files at the root of the archive, not the folder
itself) and upload it under Ghost → Settings → Design → Change theme.
