{{!-- What a desk is for.

      The tag description in Ghost Admin is the real source: an editor
      can change it without touching the theme. This is only the
      fallback, so a desk that has no description written yet still
      reads as a section rather than as an empty page. Held in one
      place so the two never drift apart. --}}
{{~#match slug "technology"}}Contracts, substations, defaults and models. The decisions that get made long before anyone thinks to vote on them.{{/match~}}
{{~#match slug "philosophy"}}Attention, consent, responsibility, judgment. The words you need when the thing going wrong is legal, efficient and working exactly as designed.{{/match~}}
